machine:
  python:
      version: 2.7.11, 3.5.1

dependencies:
  override:
    - pip install -U pip
    - make dev

test:
  pre:
    - make info
    - uname -a
    - lsb_release -a
  override:
    - make coverage
